import * as React from "react";
import { Text, View } from "react-native";

export default function ExchangeModal() {
	return (
		<View style={{ height: 500, justifyContent: "center", alignItems: "center", backgroundColor: "white", position: "absolute", bottom: 0, width: "100%" }}>
			<Text>Exchange Modal!</Text>
		</View>
	);
}
